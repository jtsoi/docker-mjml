Docker build for MJML.io using alpine linux
===========================================

Download at [https://hub.docker.com/r/jtsoi/mjml/]()

Example usage:

    >docker run -it -v $(pwd):/root jtsoi/mjml -r test.mjml -o test.html
